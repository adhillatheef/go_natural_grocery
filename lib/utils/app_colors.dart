import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (buttons, highlighted text, selected icons)
  static const Color primary = Color(0xFF7C2F02);

  // Text colors
  static const Color textMain = Color(0xFF222222);
  static const Color textDisabled = Color(0xFF8D8D8D);

  // UI Element colors
  static const Color cardBackground = Color(0xFFF2F2F2);
  static const Color border = Color(0xFFDFDFDF);

  // Standard colors
  static const Color background = Colors.white;
  static const Color error = Colors.red;
}